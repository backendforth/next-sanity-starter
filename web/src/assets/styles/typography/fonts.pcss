/* Three woff2 in `public/fonts/` — match `url("/fonts/…")` + `@font-face` names to `--font-family-*`.
   sans+text → sans face; serif+headline → serif; mono alone. Or use next/font/local and drop @font-face. */

@font-face {
  font-family: "PlaceholderSans";
  src: url("/fonts/REPLACE-WITH-SANS.woff2") format("woff2");
  font-weight: 100 900;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "PlaceholderSerif";
  src: url("/fonts/REPLACE-WITH-SERIF.woff2") format("woff2");
  font-weight: 100 900;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "PlaceholderMono";
  src: url("/fonts/REPLACE-WITH-MONO.woff2") format("woff2");
  font-weight: 100 900;
  font-style: normal;
  font-display: swap;
}

:root {
  --font-family-sans: "PlaceholderSans", ui-sans-serif, system-ui, sans-serif;
  --font-family-serif: "PlaceholderSerif", ui-serif, Georgia, serif;
  --font-family-mono: "PlaceholderMono", ui-monospace, monospace;

  --font-family-text: var(--font-family-sans);
  --font-family-headline: var(--font-family-serif);
}
