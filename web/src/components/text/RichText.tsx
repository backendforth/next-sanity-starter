/**
 * Placeholder for future UI that targets Studio schema **`richText`** only
 * (standard blocks/annotations — **no** embedded `module.*` types).
 *
 * **Default path:** use **`RichTextMedia`** from `./RichTextMedia` for `richTextMedia` /
 * `internationalizedArrayRichTextMedia` (module bodies, etc.).
 */

export {};
