/**
 * Re-exports from `@repo/languages` — keep `sanity.config.ts` imports stable.
 * Edit **`packages/languages/src/index.ts`** for ids, default, and Studio labels.
 */
export {
  defaultLanguageIds,
  studioLanguages,
} from "@repo/languages";
